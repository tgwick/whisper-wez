# Runtime artifacts
state.json
whisperwez.log
*.log

# Bundled binary is large; document how to fetch it in README instead of committing
# (remove this line if you prefer to vendor sqlite3.exe directly)
sqlite3.exe
